import { StimPayloadDirective } from './stim-payload.directive';

describe('StimPayloadDirective', () => {
  it('should create an instance', () => {
    const directive = new StimPayloadDirective();
    expect(directive).toBeTruthy();
  });
});
