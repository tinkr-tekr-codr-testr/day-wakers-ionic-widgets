import { StimIconDirective } from './stim-icon.directive';

describe('StimIconDirective', () => {
  it('should create an instance', () => {
    const directive = new StimIconDirective();
    expect(directive).toBeTruthy();
  });
});
