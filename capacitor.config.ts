import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a-day-wakers-ionic-widgets',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
